# Дневник разработки

## 2026-05-17
- Создан дневник разработки для фиксации выполненных шагов по коду и анализу проекта.
- Начат аудит соответствия приложения требованиям SkillFlow: сначала изучение доступной документации (кроме README), диаграммы и исходного кода.
- Изучена основная документация проекта в файле `SkillFlow Project.docx` (без README, как запрошено).
- Изучена диаграмма `skillflow.drawio.svg`, включая разделы по паттернам и модулям.
- Проведена проверка реализации в ключевых модулях `app/api`, `app/services`, `app/models`, `bot`.
- Запущен `pytest -q`: тесты не стартуют из-за отсутствующих зависимостей (`sqlalchemy`) в окружении.
- Подготовлен вывод о соответствии обязательств и паттернов: частичное соответствие, есть явные расхождения с ТЗ/диаграммой.

---

## Требования для перехода от MVP к полноценному проекту начального уровня

Ниже зафиксированы обязательные требования, выполнение которых переводит SkillFlow из состояния MVP в состояние устойчивого проектного решения начального уровня.

### 1) Функциональная полнота (обязательно)
- Полный рабочий сценарий для студента: регистрация → создание теста → отправка на модерацию → публикация преподавателем → прохождение теста → результат → рейтинг → достижения.
- Полный рабочий сценарий для преподавателя: регистрация → закрепление предметов → очередь модерации по своим предметам → approve/reject с комментарием → просмотр аналитики.
- Telegram-сценарии должны быть согласованы с web API: link-code, просмотр тестов, открытие теста, отправка попытки, получение статистики и рейтинга.
- Все 4 типа вопросов должны корректно работать end-to-end: SINGLE_CHOICE, MULTIPLE_CHOICE, TEXT_ANSWER, MATCHING.

### 2) Формальные бизнес-правила (обязательно)
- Проходить тесты может только студент.
- Проходить можно только опубликованные тесты.
- Повторная попытка возможна только при явном подтверждении (`allow_retake`).
- Модерация доступна только преподавателю и только по закреплённым дисциплинам.
- Тест создаётся в статусе DRAFT и меняет состояние только через допустимые переходы state machine.
- Рейтинг считается по предметам, а повторная попытка корректирует рейтинг через дельту.
- Достижение не может выдаваться повторно одному и тому же пользователю.
- Telegram link code имеет TTL и инвалидируется после использования/истечения.

### 3) Архитектурная дисциплина (обязательно)
- Слои должны быть разделены: API → Services → Repositories → Models.
- Вся бизнес-логика находится в service-слое, а не в роутерах и не в Telegram-боте.
- Репозитории не содержат бизнес-правил, только доступ к данным.
- EventBus используется как механизм расширения (новые реакции добавляются подписчиками без изменения core-flow).
- Реализация паттернов должна быть не только «декларативной», но и реально использоваться в рабочих сценариях:
  - Repository
  - State Machine
  - Observer/EventBus
  - Strategy
  - Factory
  - Builder
  - Visitor
  - Adapter
  - Dependency Injection

### 4) Целостность данных и транзакционность (обязательно)
- Все критические операции (создание теста, прохождение теста, модерация, пересчёт рейтинга, выдача достижений) выполняются в управляемых транзакционных границах.
- При любой ошибке система делает rollback без «полусохранённых» сущностей.
- Уникальные ограничения в БД должны гарантировать:
  - уникальность email,
  - уникальность telegram_id,
  - уникальность пары student-subject в rating,
  - уникальность пары student-achievement.

### 5) Валидация и безопасность (обязательно)
- Входная валидация должна ловить неконсистентные payload до сервисного слоя.
- Валидация профиля должна проверять связки институт/кафедра/направление по каталогу.
- Пароли хранятся только в виде хэша.
- Доступ к защищённым методам — только по JWT и RBAC-проверкам.
- Для Telegram-интеграции должен быть явный trust boundary (бот не получает прямого доступа к БД).

### 6) Тестирование как критерий готовности (обязательно)
- Unit-тесты: state machine, scoring visitor, strategy selection, achievement rules, catalog validation.
- Integration/API-тесты: auth, create/submit/moderate/attempt flows, ratings, stats, telegram connect.
- Регрессионные тесты на ретейк, дельту рейтинга, ивенты TEST_COMPLETED/TEST_PUBLISHED.
- Минимальный порог: тесты должны запускаться в CI и быть зелёными в чистом окружении.

### 7) Эксплуатационная готовность (минимальный production-ready уровень)
- Обязательный CI pipeline: install → lint → test → build.
- Структурированные логи по ключевым событиям (user_id, test_id, event_name, status).
- Прозрачный процесс миграций (Alembic) без ручных «магических» изменений схемы.
- Документированный процесс запуска (локально и Docker Compose) и healthcheck.

### 8) Нефункциональные критерии для «проекта, а не MVP»
- Предсказуемое поведение под умеренной нагрузкой (без деградации рейтингов и модерации на десятках/сотнях пользователей).
- Отсутствие критических runtime-ошибок в основных сценариях.
- Единые контракты API для web и Telegram каналов.
- Повторяемая сборка и запуск новой среды без ручных исправлений кода.

---

## Definition of Done (готовность начального проектного уровня)
SkillFlow считается не MVP, а проектом начального уровня только если одновременно выполнено:
1. Все обязательные бизнес-сценарии работают end-to-end.
2. Все ключевые бизнес-правила подтверждены автотестами.
3. Архитектурные паттерны реально задействованы в production flow.
4. Данные защищены транзакциями и БД-ограничениями.
5. Безопасность (hash/JWT/RBAC/TTL) реализована и протестирована.
6. CI стабильно запускает тесты в чистом окружении.
7. Документация и запуск воспроизводимы для новой команды без «устных договорённостей».

- Улучшена конфигурируемость Telegram link-code: TTL вынесен из «магической константы» в настройки приложения (`telegram_link_code_ttl_seconds`), чтобы менять срок жизни кода через окружение без правок кода.
- Маршрут `/api/telegram/link-code` переведён на использование TTL из `settings`, поведение API сохранено, но стало управляемым через `.env`.
- Выполнена проверка тестов (`pytest -q tests/test_services.py`) — запуск в текущем окружении блокируется отсутствующей зависимостью `sqlalchemy` (в requirements присутствует, но не установлена в runtime контейнера).
- Усилена валидация Telegram-контрактов на уровне схем: код привязки теперь строго 6 цифр, `ttl_seconds` не может быть отрицательным, для `telegram_id` добавлены разумные границы длины.
- Добавлены unit-тесты схем (`tests/test_schemas.py`) на негативные кейсы Telegram-валидации.
- Существенно усилен Telegram flow: добавлена проверка коллизий при генерации link-code, добавлены явные 409-ответы для уже привязанного аккаунта/telegram_id, а также закрыт риск bot-аутентификации при пустом `TELEGRAM_BOT_TOKEN`.
- Выполнен крупный пакет доработок Telegram за один проход: добавлены команды `/mytests` и подтверждаемый `/retake`, улучшена UX-обработка ошибок API, усилена валидация кода привязки в боте и адаптере, а также добавлен безопасный in-memory сценарий подтверждения повторной попытки.
- Выполнен крупный этап не только по Telegram: усилены правила модерации (reject теперь требует осмысленный комментарий в схемах и сервисе), убран анти-паттерн `raise exc`/лишний try-catch в `TestService`, добавлен CI pipeline (`install → compile → pytest`) и обновлена README-документация по env/новым bot-командам.
- Новый крупный шаг по эксплуатационной готовности: добавлены структурированные логи EventBus-публикаций, readiness-эндпоинт `/health/ready` с проверкой подключения к БД, healthcheck для `app` в docker-compose и поддержка `curl` в образе для корректной работы probe.
- Ещё один большой этап: добавлен middleware с `X-Request-Id` и логированием latency/status для каждого HTTP-запроса, улучшено поведение `/health/ready` (503 + JSON при проблеме БД), оптимизирован перерасчёт позиций рейтинга без N отдельных update-запросов, расширены проверки в CI и добавлен тест `test_rating_repository_recalculates_positions_after_updates`.
- Закрыт ещё один крупный блок требований по корректности доменной логики: в `take_test` добавлена строгая проверка целостности набора ответов (без дублей, без посторонних `question_id`, с обязательным покрытием всех вопросов теста), добавлены отдельные тесты `tests/test_attempt_validation.py` и расширено событийное логирование ключевых действий модерации/прохождения.
- Дополнительно усилены правила регистрации: email теперь нормализуется в lower-case при регистрации/логине, а пароль валидируется строже (мин. 8 символов + обязательные буква и цифра). Добавлен тест `tests/test_auth_validation.py` на негативный кейс слабого пароля.
- Закрыто дополнительное бизнес-правило: создание тестов ограничено только студентами (преподаватели не могут создавать тесты), что синхронизирует web/telegram-поведение с требованием по ролям; добавлен тест-кейс на запрет создания теста преподавателем.
- Максимально закрыт ещё блок доменных ограничений: добавлена ролево-зависимая консистентность профиля при регистрации (teacher/student поля взаимоисключены), добавлена новая миграция с индексами для ускорения telegram-link и поиска предыдущих попыток, расширены тесты валидации авторизации/регистрации.
- Добавлена строгая role-based валидация регистрационного профиля на уровне схем: для STUDENT обязательны группа/курс/код направления, для TEACHER обязательны кафедра и список дисциплин. Добавлены соответствующие негативные тесты в `tests/test_auth_validation.py`.
- Финальная полировка совместимости с `main`: добавлен `MERGE_PLAYBOOK.md` с пошаговым сценарием rebase/resolve/migration/smoke-check/CI, README обновлён ссылкой на playbook для бесшовного merge.
